const { formatErrorMessage } = require("../utils/display");

function unknownCommand(program) {
  program.on("command:*", function () {
    console.error(
      formatErrorMessage(`Invalid command: ${program.args.join(" ")}`)
    );
    program.outputHelp();
    process.exit(1);
  });
}

module.exports = unknownCommand;
