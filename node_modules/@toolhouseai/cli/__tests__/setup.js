// Mock environment variables
process.env.TOOLHOUSE_API_URL = 'http://mock-api.test';
process.env.GENERATION_SERVICE_URL = 'http://mock-gen.test';
process.env.TOOLHOUSE_URL = 'http://mock.test';
process.env.TOOLHOUSE_ENV = 'test';
process.env.AGENT_MODEL = 'test-model';

// Mock file system
jest.mock('fs');

// Mock node-fetch
jest.mock('node-fetch');

// Mock open
jest.mock('open');

// Mock chalk to prevent color output in tests
jest.mock('chalk', () => ({
  red: jest.fn((text) => text),
  green: jest.fn((text) => text),
  blue: jest.fn((text) => text),
  cyan: jest.fn((text) => text),
  yellow: jest.fn((text) => text),
  dim: jest.fn((text) => text),
  bold: jest.fn((text) => text)
}));